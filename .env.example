# ---------------------------------------------------------------------------
# Once Was Yours — root/tooling environment.
# App runtime env lives in apps/web/.env.example. This file holds values used by
# CLI tooling (Supabase, CI) at the repo root. NEVER commit real secrets.
# ---------------------------------------------------------------------------

# Supabase CLI (link/push migrations to a cloud project — no Docker required)
SUPABASE_ACCESS_TOKEN=
SUPABASE_PROJECT_REF=
SUPABASE_DB_PASSWORD=
