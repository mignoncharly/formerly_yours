packages:
  - "apps/*"
  - "packages/*"

# @sentry/cli's postinstall downloads a binary needed only for build-time
# source-map upload (currently disabled). Don't run it.
allowBuilds:
  "@sentry/cli": false
